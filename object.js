const user = [];
user.name = 'Adnan';
user.ocupation = 'Programmer';

const Programmer = {
   name: 'Hassan',
   job: 'programmer',
   salary: 10000
};

const user = new object();


const item = object.create(null);

/**
 * way to create object 
 * object literl
 * object construcctor
 * object constructor function
 * Es6 classes
 * object.create()
 * */ 